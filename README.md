# Installation
```
# you need node.js with npm installed on your machine
$ npm install -g less gulp-cli # global tools
$ npm install # dependencies of this website
$ cd semantic && gulp build # build semantic-ui
$ npm run watch # watch files and bulid them automatically
```
